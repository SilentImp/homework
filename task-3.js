/**
 * Знайдіть добуток масиву чисел з невідомою глибиною вкладеності:
 * @param {Number[]|Number[][]} resultsArray — число
 * @returns {Number} Добуток всіх елементрів масиву
 * @example
 * task([1, 2, [3, [4]]]);
 * // returns 24
 * @example
 * task([2, 2]);
 * // returns 4
 */

const task = (resultsArray) => {
  let productOfArray;

  // тут ваш код ...

  return productOfArray;
};

module.exports = task;