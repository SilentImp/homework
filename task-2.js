/**
 * Задача на розворот числа:
 * @param {Number} currentMaxValue — число
 * @returns {Number} перевернене число
 * @example
 * task(4589);
 * // returns 9854
 * @example
 * task(1234);
 * // returns 4321
 * @example
 * task(111);
 * // returns 111
 */

const task = (currentMaxValue) => {
  let reverseMaxValue;

  // тут ваш код ...

  return reverseMaxValue;
};

module.exports = task;